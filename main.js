/*let numero = 5;

function suma(numero){
        let total = numero + numero
        console.log(total)
    return total
}

suma(150)*/

//Para crear un Json package, el comando "npm init -y" es lo más sencillo.

const persona = { nombre: "Diego" };